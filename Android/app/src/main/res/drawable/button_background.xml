<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Change color as per your preference -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle"/>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/main_color" /> <!-- Change color as per your preference -->
            <corners android:radius="8dp" /> <!-- Change radius as per your preference -->
        </shape>
    </item>
</ripple>




